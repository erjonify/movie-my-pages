import styled from 'styled-components'
import { flexCenterContent } from '../../../styles/mainTheme'

export const Button = styled.button`
    ${flexCenterContent};
    background: transparent;
    border: none;
    padding: 0;
`
